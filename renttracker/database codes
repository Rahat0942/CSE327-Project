CREATE TABLE users(
	usersId int(30) PRIMARY KEY AUTO_INCREMENT NOT NULL,
    
    usersName varchar(128) NOT NULL,
    userEmail varchar(128) NOT NULL,
    userUid varchar(128) NOT NULL,
    userPwd varchar(128) NOT NULL    
);
